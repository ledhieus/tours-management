mixin formatDateTime(item)
    span #{moment(item).format("DD/MM/YYYY HH:mm:ss")}

mixin formatDate(item)
    span #{moment(item).format("DD/MM/YYYY")}

mixin formatTime(item)
    span #{moment(item).format("HH:mm:ss")}